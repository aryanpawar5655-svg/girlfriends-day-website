# Background music

Drop a single MP3 here, named exactly `song.mp3`.

Keep it under ~5MB so GitHub Pages loads it fast. If the file is missing, the music button just says "Add song.mp3" for a moment — the rest of the site works fine.
